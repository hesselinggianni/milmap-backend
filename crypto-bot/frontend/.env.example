# Base URL of the Laravel API. Op 8001 gezet zodat het niet botst met milmap
# (dat vaak op 8000 draait). Start de backend met: php artisan serve --port=8001
VITE_API_BASE_URL=http://localhost:8001/api

# Must match BOT_API_TOKEN in the backend .env (leave empty for local dev).
VITE_API_TOKEN=
