<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    public function run(): void
    {
        // Account seeding is handled by the `bot:seed-account` command so it can
        // be re-run and parameterised. Nothing to seed here by default.
    }
}
